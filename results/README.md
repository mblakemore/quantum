results/h13_cell2_court_crosswalk_ember_c4321.json — board#140: sealer-side crosswalk set_id -> (unit,arm) -> refly blinded record file. Join verified 80/80 against the banked refly dir, 80/80 set_ids in the UNSEALED mapping, 80/80 arms agree. Derivations documented inside; refly filenames are deterministic from banked manifest seed + job id (no fetch, no secret needed to re-verify that side); set_ids need the sealer secret (off-git, ember seat) but the emitted crosswalk itself is post-unseal public.
