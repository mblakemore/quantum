{
 "_meta": {
  "card": "docs/door-a-stabilizer-memory-separation-prereg-DRAFT-whisper-c5027.md",
  "assembled_by": "Elder C6593 (grader seat), on Creator's freeze GO verified against results/doora_tau_c1_freeze_whisper_c5027.json",
  "status": "FROZEN except fields marked TO_FILL_AT_SUBMISSION — the grader REFUSES on any unfilled field at grade time; filling them happens BEFORE flight inside the lambda-provenance flow, never from flight data",
  "contract": "v3",
  "fill_ordering": "BINDING (Ember #6485): (1) epoch-lambda measured on the flown register at the submission window -> lambda_provenance; (2) THEN u_est, tau_Q, Q grids, noise-only curves derived FROM that lambda; (3) THEN submit. Pre-computing derived fields from an earlier lambda is a stale number wearing a frozen field — the grader's derived-from assertion (check_derived_from) recomputes lambda -> u_est -> tau_Q per rung and REFUSES on mismatch, so provenance is checked, not just presence."
 },
 "bit_layout": "halves",
 "epsilon_trial": 0.01,
 "power": 0.9,
 "alpha_power_court_ratified": true,
 "reporting_ci_level": 0.95,
 "reporting_ci_note": "two-alphas rule (#6320): epsilon_trial sizes shots; reporting CIs (Clopper-Pearson rows, excess-fit) are 95% two-sided. Unqualified 'alpha' is banned from this file.",
 "criterion": 0.95,
 "trials_per_rung": 40,
 "execution_path": {
  "primitive": "SamplerV2 (ISA circuits; V2 does not transpile — no stage exists to configure, Ember #6374)",
  "dynamical_decoupling": {
   "enable": false,
   "reason": "reproducibility + clean noise-only curves, NOT blindness (idle structure is branch-identical post-template)"
  },
  "twirling": {
   "enable_gates": false,
   "enable_measure": false
  },
  "simulator": "MUST_BE_UNSET — hard assert at submission (a stray value produces a real-looking flight that never touched hardware)",
  "doc_check_citation": "qiskit-ibm-runtime 0.47.0 / qiskit 2.4.1. ISA requirement CONFIRMED (guides/transpile). No-server-retranspile NOT AFFIRMED by public docs as of 2026-08-08. HOWEVER the risk is STRUCTURALLY CLOSED by the PUB shape, VERIFIED FROM SOURCE (Whisper #6493): SamplerV2 receives (circuit, parameter_values, shots) with the circuit UNBOUND, so any server-side transpilation acts on a circuit whose angles have no values and cannot delete angle-0 slots — the identical fix as client-side bind-after-transpile, one layer out. Residual risk = bind-then-reoptimise-per-parameter-set (binding is normally a pulse-parameter update, not a circuit rewrite), tested by the pilot timing probe (Ember) SUBJECT TO a metrics-granularity pre-check: if job usage/metrics resolve only per-JOB, the timing probe is uninformative and must say so rather than pass vacuously; the u-consistency probe (grader-side) covers the residual regardless of metrics granularity."
 },
 "staged_submission": {
  "pilot_rung": 8,
  "continue_rule": "COST-ONLY (measured per-shot QPU rate) — outcome-independent by construction; NO rung unsealed until the flight set is closed (card f1e8042)",
  "pilot_u_consistency_probe": "DEMOTED TO WEAK BACKSTOP, v3 (Whisper #6627 + Elder's own band mis-sizing owned): the realistic fault under the swap-network kit is PARTIAL elision — only angle-0 CPs can delete, SWAPs never elide, so ~25% elision (all-zero-angle slots, A half-zero on average) gives u=0.5069, which sits INSIDE both plausible favourable calibration drift (20% -> 0.4844) AND the +0.15 band (halt at 0.554). Elder's v2 band was sized against the OLD leaky-variant full-deletion figure (~0.99) — the wrong fault model for this kit; stated plainly rather than quietly resized. The u check REMAINS as an asymmetric weak backstop (>=50% elision at 0.6357 still clears the band and halts) but NOTHING may rely on it. WHAT CARRIES THE BLINDNESS (measured, direct): G-C — all-zero vs all-one bind IDENTICAL on the real flown object.",
  "pilot_timing_weight_probe": "SECOND pre-registered service-behaviour probe (Ember #6489, submitter-side, touches nothing sealed): per-trial execution timing from RuntimeJobV2 metrics checked against A/A-prime Hamming weight — the weight variation exists naturally (NULL draws fresh A-prime per copy). FLAT timing across weight = service not deleting slots; timing TRACKING weight = the leak is live at the server, caught on a 4-8s pilot instead of in a published claim. If the timing signal is too coarse to resolve, that is learned and the declaration stands as-is, weaker but honestly labelled. TWO instruments on the same question: this (structural/timing, submitter-side) + pilot_u_consistency_probe (fidelity/statistics, grader-visible). If BOTH return clean, execution_path.doc_check_citation upgrades from declaration to MEASURED-AT-PILOT.",
  "per_rung_bind_invariant": "RULING (Elder, on Ember #6566): full per-rung empirical weight sweeps are NOT required as blocking gates — the branch/weight leak is closed by ORDERING (assign_parameters substitutes, cannot run passes, cannot delete; count fixed at transpile time for every binding at every size), verified empirically at n=4/6 incl. the all-zero worst case, with the bind-early path raising at every site by enumeration. REQUIRED IN THEIR PLACE, per rung, AT SUBMISSION, on the ACTUAL FLOWN ISA artifact: (i) assert the transpiled object has free parameters > 0 (it was transpiled unbound); (ii) two-point spot check — bind ALL-ZERO and ALL-ONE vectors on the flown object and assert EXACT count and depth equality. Seconds per rung, placed on the production path at the submission epoch — the right-place form of the retired sweep. A rung failing the spot check is a fly-blocker exactly as the sweep would have been. CLARIFICATION (Whisper #6569, adopted): what is retired is the WEIGHT-SWEEP only. The per-rung TRANSPILE-WHAT-YOU-FLY step is NOT retired and cannot be — the actual submitted circuit is transpiled at every rung with the layout actually used (this is also where template_joint_isa_2q and its provenance come from), and the two-point invariant runs ON THAT OBJECT. The two are easy to confuse because the same command produces both; they are different requirements and only one of them was retired.",
  "upper_rung_fill_timing": "RULING (Elder, on Whisper #6576 option set): (a) ADOPTED — the pilot is n=8 only and n=8 is measured (384 2q, 3.20x, 89 shots re-quoted); n=12/16 measurements run in the background and their fills are needed only if the ladder continues past the pilot, a decision the staged-submission rule already places AFTER the pilot's measured QPU rate. FALLBACK SEMANTICS if that decision arrives before the measurements land: reduced-effort transpile numbers labeled as upper bounds are admissible for the COST-ONLY continue/stop decision (an upper bound is conservative for a budget call) but are NEVER admissible as template_joint_isa_2q fills — the fill takes only the actual-effort artifact number of the circuit that would fly. Option (c) proxy-extrapolation REFUSED: three points do not fix a trend, per the reporter's own recommendation.",
  "pilot_budget_assertion": "SUPERSEDED — the gate as ratified was UNREADABLE: usage_estimation is a property of RuntimeJobV2, which exists only after submission; SamplerV2 exposes no dry estimate (Ember #6611). The seat that ratified the threshold (Elder) and the seat that proposed the read both argued about WHERE to set a line without checking the line existed — the u>=0.70 class, on our own gate. Replaced by micro_prefix_estimate below.",
  "budget_contingency_prefix_M": "PRE-RULED BEFORE THE ESTIMATE EXISTS (Elder, on Whisper #6591's design consequence — moved before data, as required): IF usage_estimation fails the 50%-of-remaining gate, the ONE pre-registered fallback is a SEALED-PREFIX flight at M'=20 trials per rung — the first 20 of the 40 sealed labels (seal-compatible: the commitment binds the full sequence, a prefix flight leaves unflown labels sealed and the seal verifying). Criterion re-derives to >=19/20 (=95%), attainment 0.9831 at epsilon_trial=0.01 (vs 0.9925 at M=40 — still comfortably instrument-dominated), CIs wider and REPORTED as such. NOTHING ELSE MOVES: same tau's, same grids, same fence (shots=1 stays — the fence is the blindness, not a knob). If even M'=20 fails the budget gate, REFUSE AND RETURN TO COURT — there is no second console reduction. The cost, if large, is the FENCE's per-circuit term; learning its size is itself a pilot deliverable.",
  "micro_prefix_estimate": "RULED v2 (Elder, adopting Whisper #6613's trial-boundary condition — supersedes my K=16-rows spec, which could land arm-disproportionate: Q rows are heavy 16q/372-gate objects, C1 rows light, and a row-count slice measures a rate that exists for neither arm). THE PROBE = EXACTLY ONE SEALED TRIAL (trial 1, both arms, 77 Q + 160 C1 executions in emission order) submitted as its own job. WHY ONE TRIAL: every trial has the same arm mix, so the projection is assumption-free arithmetic — usage(trial 1) x 39 remaining trials <= 50% of the instance's remaining seconds. No per-arm rate decomposition, no blend correction; the prefix has the target's shape by construction (the property the anchor jobs lacked). BIAS: per-job overhead amortized over one trial OVER-states the projection — over-refuses, never under-refuses. PROBE COST ACCEPTED IN ADVANCE: realistic sub-second (floor rate ~0.06-0.2s/trial); the pathological case (~12s at 50ms/circuit) is itself decisive information and is the unavoidable discovery cost — no smaller unit preserves the assumption-free projection. FALLBACK LADDER unchanged: full-40 projection fails -> evaluate M'=20 projection -> fails -> REFUSE TO COURT. EXCHANGEABILITY RULING (Whisper's explicit ask): the probe trial's data COUNTS toward the result — it is the sealed schedule flying in order; discarding it would silently shrink M. Each trial's job_id is recorded in flight.json, and the grader reports accuracy WITH and WITHOUT probe-job trials as a pre-registered SENSITIVITY row (reported, never gating) so any cross-job condition drift is visible rather than assumed away. Whisper's copies-vs-executions correction (9,480 executions, not 12,560) is absorbed with no ratified number moving: the 3x leg was already demoted to a deliverable and the 50% ceiling binds on measured projection regardless. PROJECTION FORMULA v3 (Elder ruling on Whisper #6695's HOLD — a ratified gate changes, so it is ruled not patched): the x39 naive projection charges a FIXED PER-JOB COST forty times — the throwaway measured job overhead ~2s, LARGER than the entire projected execution budget, so naive usage(probe) x 39 = ~79-82s would REFUSE a flight that fits in <5s: a false stop from our own safety gate. CORRECTED, pre-registered: projected_remaining = JOB_OVERHEAD + 39 x (usage(probe) - JOB_OVERHEAD), gated against 50% of remaining, with JOB_OVERHEAD = the throwaway's measured overhead. The assumption-free property SURVIVES where it mattered (no rate decomposition INSIDE the trial); the fix merely charges each cost at its own multiplicity — per-job x1, per-trial x39. SIGN CORRECTION TO #6695's caveat 2, shown by algebra: if the 39 fly as one job, true cost = OH_true + 39T and P - true = 38 x (OH_true - OH_assumed). A FLOOR overhead (OH_assumed <= OH_true, plausible since a trivial job's overhead lower-bounds a large job's) makes the projection OVER-estimate -> the gate OVER-refuses -> the SAFE direction; under-estimation requires subtracting MORE overhead than exists. Whisper's stated direction was inverted; corrected here with the arithmetic visible. PRECONDITIONS before anyone acts: (1) Ember confirms the USAGE UNIT (if usage=2 is not seconds, all of this re-derives); (2) the throwaway overhead is recorded in the artifact as the floor it is, with the 38x sensitivity stated. GENERAL RULE for the ledger: a projection must charge each cost at its own multiplicity — the anchor-regime lesson one level up, caught by a 1-qubit job that was the right instrument by accident. HARDENING v4 (Elder ruling on Whisper #6702's proposal, mechanics corrected): his instinct — convert the 38x-leveraged overhead from a 1-qubit floor into a measurement on the real flight — is adopted, but NOT as stated: 'usage(probe) - 77*rate' cannot be computed from one job, because the calibration rows yield FIDELITY not timing, and a single job yields ONE aggregate usage — one equation, two unknowns (OH, rate). THE WORKING FORM is a TWO-JOB SPLIT: fly the 77 trial rows and the K=32 calibration rows as separate jobs -> usage_1 = OH + 77r, usage_2 = OH + 32r -> 45r = usage_1 - usage_2 solves both exactly, on real flight circuits at the epoch, at the cost of one extra job overhead (~2s of 23). RULED IN REFUSAL-TRIGGERED FORM to spend that only when it matters: (1) gate FIRST with the floor-OH projection (conservative — a PASS is final, since a conservative pass is safe); (2) a REFUSAL is NOT final until OH is measured exactly via the split and the gate re-fires ONCE with the measured OH; (3) no further re-gating — one refinement, then the answer stands. Given current headroom (corrected projection 2.8-4.5s vs the 11.5s gate) the floor version passes and the split likely never runs; it exists for the branch where the 38x leverage would otherwise kill a fitting flight on a borrowed number. Blindness unaffected by any split: jobs carry dual clocks and job_ids, rows unchanged, the sensitivity machinery already spans jobs. AMENDMENT v5 (Elder, on Whisper #6708 — his recorded-not-blocking note examined, and it wins MORE than he claimed): my v4 split had a flaw neither of us named — it would RE-FLY the 77 sealed trial rows as a second job, DUPLICATING a sealed trial in the record (which flight of trial 1 counts?), on top of the anchoring trade he did name (u_anchor separated from what it anchors, silently, on a branch that runs once under pressure). THE REFUSAL BRANCH THEREFORE RUNS HIS ALTERNATIVE: two PUBLIC-A-ONLY jobs at different row counts (32 and 100) — solves OH and r exactly with NO sealed content anywhere, no duplication, and the original probe's in-job anchoring holds UNCONDITIONALLY (the #6684 property is preserved on every branch, not traded on the rare one). Cost ~2s more than the flawed split; on a branch deciding whether a fitting flight lives, trivially justified by removing two defects at once. His deeper point is the design law and goes in the text: refusal-triggered code sits unexecuted and then runs ONCE, under pressure, at the moment a flight is about to be lost — the exact circumstance where an unstated trade becomes an unnoticed one. Diagnostics on the pressure branch carry NO sealed content and NO traded properties, by rule. FINAL CORRECTION v6 (admitted under the freeze's own exception — a DEMONSTRATED defect, Whisper #6717, and it is Elder's third multiplicity error of the stretch): the x39 charges the K=32 calibration rows 39 times, but they are a ONE-TIME per-job anchor in the remaining flight (39x77 trial + 32 cal = 3,035 executions, not 4,251). GENERAL FORM, frozen: projected_remaining = OH + (rest_executions / probe_executions) x (usage(probe) - OH) = OH + 27.84 x (usage(probe) - OH) for this design. The ruled x39 was CONSERVATIVE (over-refuse, safe) and both forms pass today's gate with 2-3x room — a flight flown on the x39 form is valid a fortiori. Multiplicity-error ledger, three instances one shape: copies-as-executions (Whisper, 32.5%), per-job overhead x39 (Elder, 18-28x), calibration rows x39 (Elder, 40%) — four cost units exist (per-shot, per-execution, per-job, per-flight) and each must carry ITS OWN multiplier; the rule has now caught three defects and stays armed. REFUSAL-BRANCH EXECUTION SPEC v7 (Elder, reconciling Whisper #6729 with the frozen branch): his stop-discipline is CORRECT and the frozen rule already embodies it — the stop is NOT YET FINAL by the pre-registered text ('a REFUSAL is not final until OH is measured exactly and the gate re-fires ONCE'), and following a branch frozen BEFORE any measurement existed is the opposite of reopening-by-argument: the bar cannot slide along a path that was ruled blind. HIS INSTANTIATION IS ADOPTED AS SUPERIOR: the diagnostic = ONE new public-A-only job of ~500 rows (~3-7s; low vs high rate produce DIFFERENT INTEGERS, collapsing the ambiguity in one job), PAIRED WITH THE ALREADY-FLOWN PROBE as the second equation: 391r = usage_500 - usage_probe -> r at ~1/391 s resolution (vs 1/68 for the 32+100 pair), OH by subtraction. Then the ONE pre-registered re-fire at 50% of remaining-at-gate; then FINAL either way — pass flies the 39, fail goes to @creator with a MEASURED top-up size. LEDGER: job.metrics timestamps are WALL-CLOCK, 4.74x billed QPU (measured on the throwaway) — they cannot substitute for usage(); negative recorded so nobody reaches for it.",
  "calibration_staleness_check": "RULED (Elder, on Ember #6624): strengthen to a TIMESTAMP comparison — before arming, read target properties' last_update_date; if it has ADVANCED past the lambda epoch, re-read lambda and re-derive (a minute, rides on the existing re-derivation step). A VALUE comparison against a cached record is structurally incapable of detecting what it checks — two reads of one snapshot agreeing is mutual confirmation, as the reporter herself caught. RESIDUAL, honestly stated: hardware drift BETWEEN publications is invisible to this API entirely; it is covered not by any pre-flight read but by the halt band below.",
  "executed_circuit_readback": "ADDED (Whisper #6627, adopted): on the completed PROBE job, attempt to read the EXECUTED circuit's 2q count from job/result metadata and ASSERT == the flown ISA count (372 at n=8). G-C proves the object SENT is label-blind; this proves the object that RAN is the object sent — direct beats inferred, one field read on a job already flying. If the API does not expose executed-circuit metadata, RECORD THAT EXPLICITLY in the artifact and the u backstop remains, labeled as the weak inference it is. Never fabricate the assertion from the submitted payload — that would test the wrong end of the wire.",
  "calibration_rows_and_u_anchor": "RULED (Elder, on Ember #6629 — the 29.58h calibration age): the API cannot serve a flight-epoch lambda, so we MEASURE it, label-free, without touching the seal: THE PROBE JOB GAINS K=32 PUBLIC CALIBRATION ROWS — the Q joint circuit bound with a FIXED PUBLIC A_cal (committed in this prereg's repo, not sealed, expected accept (1+u)/2 known in closed form). Their measured accept frequency yields u_anchor = 2*freq - 1: delivered purity ON THE FLOWN REGISTER AT THE FLIGHT EPOCH, with zero label interaction (the rows are public, marked, excluded from decisions; the decoder consumes them only through the frozen u_anchor formula). WHY NOT the sealed probe trial's implied u (Ember's version): that quantity is LABEL-DEPENDENT — a NULL trial's accept rate carries no purity information — so anchoring on it would entangle the anchor with the seal. THE ARCHITECTURE SPLIT, ruled: (1) tau_Q and ALL decision thresholds STAY FROZEN as derived from the published lambda — threshold placement affects EFFICIENCY (shots to criterion), never VALIDITY (accuracy is measured against sealed labels regardless), and un-freezing a decision threshold on flight data is the one move this card never makes; the possibly-off-optimum placement is documented, its cost is shots. (2) u_pred DEMOTES TO PRIOR with its 29.58h provenance stated. (3) The NOISE-ONLY BASELINES for the excess fit re-derive from u_anchor via the frozen formulas at grading time — pre-registered here, before any data exists; the baseline is a grading comparator, not a decision input, so anchoring it on a label-free measurement breaks nothing and fixes the 30-hour anchor honestly. (4) epoch_utc provenance carries BOTH the read time AND last_update_date (Ember's fix, ratified). The G-F projection gate is UNAFFECTED (usage is timing, not fidelity). ADDENDUM (Whisper #6633, convergent within minutes — his public-A calibration is THIS construction, independently derived; two seats, one design, no conferring): (5) HIS ROBUSTNESS TABLE CONFIRMS THE SPLIT WITH NUMBERS — tau_Q stays valid (tau < p1) until lambda is ~75% worse than published; the FRAGILE field is the SHOT BUDGET (77 sized at gap 0.2001; +20% drift needs 113 — an underpowered Q arm reports an instrument failure as a physics null, the F120 shape). Therefore: (6) SHOT-BUDGET RE-QUOTE, pre-registered: after the probe returns, if u_anchor implies shots > 77 at the ratified epsilon_trial/power via the frozen sizing formula, the Q pair budget for the REMAINING 39 trials re-derives from u_anchor — an EFFICIENCY parameter under the placement-vs-validity split, never a decision threshold; tau_Q does not move. The G-F projection then uses the updated budget in its x39 arithmetic. (7) THE ASYMMETRY THAT MAKES NONE OF THIS BLOCK THE PROBE: p0 = 1/2 + 2^-(n+1) is EXACT and contains no lambda — stale-optimistic calibration can only cost POWER (false negative), never manufacture a false separation. We can lose wrongly; we cannot win wrongly. (8) EMBER'S CONDITION (#6636), made explicit and structural: the calibration rows are ADDITIONAL ROWS IN THE SAME PUB, binding THE SAME transpiled ISA object as the sealed trials — there is no second circuit to re-transpile, re-route, or diverge; public A_cal is just more entries in the parameter table of the one object G-C already certified (same 372 gates, same depth 222, at every binding). 'Verified one artifact, flew another' is excluded by there being exactly one artifact.",
  "c1_threshold_noise_amendment": "STOP HONORED — RULED (Elder, on Whisper #6642, owning the freeze flaw as mine): my Sec-1b freeze declared tau_C1 'noiseless geometry, epoch-independent, frozen now' — TRUE of the theorem-side DISTRIBUTIONS, FALSE as a flown DECISION THRESHOLD, and my own ceiling caution named the mechanism (noise pushes p_ALT toward p_NULL) without protecting the threshold from it. The arms were pre-registered against different devices: tau_Q noise-derived, tau_C1 noiseless — and the C1 error direction FLATTERS the claim (noise blinds the classical arm; a separation-shaped noise artifact is the audit's first target). AMENDMENT, before any data: (1) NO ARMING until the C1 circuit is transpiled and its ISA 2q counted — same layout, same backend; C1 has never been counted and its plausible cost (prep ~1 copy of the swap network + HH25 Clifford frame + routing) may approach the ~284-gate noise-domination boundary (p_ALT spans > tau at fidelity ~0.5). (2) The FROZEN noiseless tau_C1 values are RESCOPED as theorem-side references only. The FLOWN C1 threshold re-derives formulaically, pre-registered here: tau_C1_flown = midpoint(p_ALT_noisy(u_C1), p_NULL) where u_C1 = exp(-lambda_anchor * N2q_C1_measured) and p_ALT_noisy follows the fidelity-mixture model in #6642's table (validated against it before use); p_NULL = 1.0 is noise-invariant (uniform outcomes span regardless — the NULL side cannot move). (3) PRE-REGISTERED RESPONSE FUNCTION on the count: N2q_C1 such that p_ALT_noisy < tau_C1_flown - 0.10 (margin) -> proceed with re-derived threshold; within 0.10 of the boundary -> C1 shot budget re-sizes for the narrowed gap AND the margin is reported; p_ALT_noisy AT/ABOVE the crossing (~fidelity 0.5 / ~284 gates) -> THE C1 ARM AS DESIGNED CANNOT SEE ALT: STOP, return to court — flying a structurally blind C1 is the F119 naive-baseline error in hardware form and would manufacture the separation. (4) CORRECTION OF MY OWN #6637 SENTENCE: 'we can lose wrongly, we cannot win wrongly' was scoped to the lambda-staleness channel and was OVERBROAD as a flight property — the C1 noise channel IS a win-wrongly path; this amendment exists to close it. AMENDMENT v2 (Elder, on the measured 315 — correcting MY OWN response function's boundary): the ~284 'death threshold' was drawn where p_ALT_noisy crosses the FROZEN noiseless tau — the WRONG referent. The arm dies only when the NOISY GAP (p_NULL - p_ALT_noisy) is unresolvable; p_NULL = 1.0 is noise-invariant, and at the measured fidelity 0.4643, p_ALT_noisy ≈ 0.726 leaves gap ≈ 0.274 — WIDE (a non-spanning round is near-proof of ALT: P >= 1 non-span in 12 rounds = 0.978). THE ARM IS RESCUABLE BY RE-DERIVATION, not dead: (a) tau_C1_flown = midpoint(p_ALT_noisy(u_C1), 1.0) ≈ 0.863 at the measured count, formulaic from u_C1 = exp(-lambda_anchor * 315), model curve validated against #6642's table before use; (b) C1 round budget re-sizes for the noisy gap at the ratified epsilon_trial (order 12-14 rounds vs the noiseless 5; flight cost grows, priced by the probe's measured rate in the projection gate as always); (c) WHY THE CLAIM SURVIVES HONESTLY: the excess-fit architecture already compares each arm to ITS OWN noise-only baseline — C1's baseline uses the same u_C1, so the hardware handicap lives in the baseline, not the excess; the headline carries the C1-fidelity caveat (flown at 0.46) and the theorem-side comparison lives in the excess, as always. (d) BEFORE accepting 315 as C1's best: one layout/transpile sweep on the C1 circuit alone (single copy, no two-rail constraint — the 8q frame may route far cheaper on a different path); kill criterion 1 requires C1 at its BEST, and 315-with-a-better-option-untried is not its best. If the sweep confirms ~315, the re-derivation above proceeds; if it finds materially cheaper, all C1 numbers re-derive from the cheaper measured count.",
  "c1_leaves_hardware_ruling": "RULED (Elder, accepting Whisper #6649 IN FULL — and owning my v2 rescue path as the F119 mechanism's FOURTH costume: re-deriving tau_C1 under our own noise would have written the quantum-honest-vs-classical-handicapped asymmetry INTO the pre-registration and certified a weakened adversary as official): (1) C1 LEAVES HARDWARE. The theorem's classical adversary has PERFECT single-copy measurements; its floor is information-theoretic. A fidelity-0.46 flown C1 is weaker than the theorem permits — beating it proves less than the published theorem proves for free, and no re-derivation fixes that; it formalizes it. (2) THE WIN CONDITION RE-RESTS: (a) Q's measured copies-to-criterion, as EXCESS over Q's own noise-only baseline, consistent with FLAT in n (unchanged); (b) the comparator exponent is now the IDEAL ADVERSARY: the frozen noiseless Aaronson-Gottesman C1 (tau_C1=0.7055 world, R=1000, the freeze artifact) instantiates the theorem's Theta(n) adversary at full strength — Q's excess-exponent ~0 vs the ideal-C1 reference exponent consistent with 1. The measured-C1 excess clause is STRUCK from WIN. Kill criterion 1 re-scopes: 'C1 at its best' = the NOISELESS SIMULATED adversary; if ideal-C1's copies-to-criterion ties or beats measured-Q at any rung, retire — the fairness guard at full adversary strength. (3) The hardware C1 MAY fly later only as a labeled DEMONSTRATION (fidelity stated in the same breath), NEVER entering WIN. It is DROPPED from the pilot: cost collapses ~67.5% (6,400 of 9,480 executions leave). (4) PROBE SPEC RESTATED for pure-Q trials: trial 1 = 77 Q executions + the K=32 public calibration rows, one PUB, one object; arm-proportionality is moot (one arm); the x39 projection is unchanged in form and SIMPLER in substance. (5) Blindness unchanged: labels sealed, Q decisions by frozen rule, decisions-hash before unseal. The decoder's C1 path is RETAINED for sim-side verification and any future demonstration, unused for WIN. SWEEP ABSORBED (#6657, crossed with the ruling's announcement): C1 at its best is 208 gates / fidelity 0.6025 (free layout — the line was the Q arm's constraint, never C1's). This (a) retires the 'noise-dominated/arm dead' verdict cleanly, consistent with amendment v2, and (b) prices any future DEMONSTRATION flight, for which two conditions carry: per-register lambda measured on C1's OWN edges (a free layout uses different qubits — the Q path-edge lambda is the wrong denominator), and the flown number from the flown object (the 300-vs-315 line-layout spread is seed variance; neither is the flown figure). WHAT THE SWEEP DOES NOT DO — named before anyone feels the pull: it does NOT return C1 to hardware in WIN. The #6649 argument is FIDELITY-INDEPENDENT: at 0.60 as at 0.46, a flown C1 has imperfect measurements and is weaker than the theorem's adversary; healthy-but-sub-theorem is still sub-theorem, and even a perfect-fidelity flown C1 would add nothing to WIN that the exact ideal simulation does not already provide. The pull to reinstate a repaired arm is the fifth costume, and this sentence exists so it is refused by text rather than by memory.",
  "max_submit_to_execute_window": "RESCOPED after Whisper #6688 crossed the ruling (his withdrawal of his own proposal, my encoding of it, same minutes): his residual analysis is ADOPTED — the probe-delay exposure is ONE trial in forty, bounded at -0.005 attainment even in total failure, false-negative direction, inside existing tolerance; a VALIDITY gate for that residual would be machinery for its own sake, and adding gates nothing needs is its own failure mode. THE WINDOW THEREFORE DEMOTES from pre-registered gate to OPERATING PROCEDURE, kept for the exposure his analysis does not cover: the MAIN submission's 39-trial shot budget is fixed at submit from the probe's u_anchor — a late-running main job is a 39/40 exposure to mis-sizing. Direction still safe (false negative; we lose wrongly, never win wrongly) so it threatens SPEND, not validity: procedure = if execution has not begun within 30 minutes, cancel-recheck-resubmit AS COST HYGIENE (an honestly wasted flight is still a wasted flight). Not a gate; nothing refuses on it; the console may exercise judgment here because only money is at stake, never the claim. TWO PERMANENT PIECES: (1) K=32 calibration rows fly in EVERY submission, not only the probe — physics self-anchoring universal at trivial cost; (2) dual-clock provenance (submit time AND execute time) on every job, so a late run is VISIBLE rather than silently mis-stamped — Ember's fix, sufficient for the record."
 },
 "rungs": [
  {
   "n": 8,
   "tau_C1": 0.7055,
   "tau_C1_provenance": {
    "p_ALT": 0.415,
    "se_ALT": 0.0156,
    "p_NULL": 0.996,
    "se_NULL": 0.002,
    "R": 1000,
    "instrument": "stabilizer-sampling (#6472 option d)",
    "artifact": "results/doora_tau_c1_freeze_whisper_c5027.json"
   },
   "copies_per_round": 32,
   "C1_round_grid": [
    1,
    2,
    3,
    4,
    5
   ],
   "tau_Q_rule": "midpoint of (p0 = 1/2 + 2^-9, p1 = (1 + u_est)/2), u_est from template ISA count x epoch lambda",
   "tau_Q": "TO_FILL_AT_SUBMISSION",
   "Q_pair_grid": "TO_FILL_AT_SUBMISSION (exact two-proportion sizing at epsilon_trial=0.01, power 0.90, epoch u_est)",
   "Q_noise_pred": "TO_FILL_AT_SUBMISSION",
   "C1_noise_pred": "TO_FILL_AT_SUBMISSION",
   "lambda_provenance": {
    "lambda": "TO_FILL_AT_SUBMISSION",
    "epoch_utc": "TO_FILL_AT_SUBMISSION",
    "register": "TO_FILL_AT_SUBMISSION",
    "window_id": "TO_FILL_AT_SUBMISSION"
   },
   "template_joint_isa_2q": "FILL_AT_TRANSPILE_FREEZE — MEASURED per rung on the real coupling map with the line layout actually supplied (epoch-independent geometry). No closed form exists for a buildable layout; deriving it was the error (#6541).",
   "u_est": "TO_FILL_AT_SUBMISSION (= exp(-lambda * template_joint_isa_2q); grader recomputes and refuses on mismatch)",
   "layout_spec": "SUPERSEDED->LINE (Whisper #6541: the two-rail ladder is UNCONSTRUCTIBLE on heavy-hex — ZERO 4-cycles in the coupling graph, by design; ladder_layout kept in-kit as a documented impossibility). Q arm flies the LINE layout supplied explicitly via line_layout(cm, 2n) (backtracking, fails loudly). Bell pairs therefore route; the cost is NOT closed-form and MUST BE MEASURED.",
   "template_joint_isa_2q_expected_note": "ONE-SIDED sanity check only: the measured count must be >= 120 (the unconstructible-ladder bound = swap networks + n Bell edges with zero routing — any measured count BELOW it is impossible and REFUSES). The former exact-equality requirement (103404e) pinned an impossible layout and is SUPERSEDED — it was fail-safe (refused everything) but unsatisfiable. Direction of the derivation error was FLATTERING (understated cost -> over-predicted u -> optimistic tau_Q); it never froze because the slot was empty — fill discipline 3-for-3.",
   "template_joint_isa_2q_lower_bound": 120
  },
  {
   "n": 12,
   "tau_C1": 0.7205,
   "tau_C1_provenance": {
    "p_ALT": 0.441,
    "se_ALT": 0.0157,
    "p_NULL": 1.0,
    "se_NULL_upper": 0.001,
    "R": 1000,
    "instrument": "stabilizer-sampling (#6472 option d)",
    "artifact": "results/doora_tau_c1_freeze_whisper_c5027.json"
   },
   "copies_per_round": 48,
   "C1_round_grid": [
    1,
    2,
    3,
    4,
    5
   ],
   "tau_Q_rule": "midpoint of (p0 = 1/2 + 2^-13, p1 = (1 + u_est)/2), u_est from template ISA count x epoch lambda",
   "tau_Q": "TO_FILL_AT_SUBMISSION",
   "Q_pair_grid": "TO_FILL_AT_SUBMISSION",
   "Q_noise_pred": "TO_FILL_AT_SUBMISSION",
   "C1_noise_pred": "TO_FILL_AT_SUBMISSION",
   "lambda_provenance": {
    "lambda": "TO_FILL_AT_SUBMISSION",
    "epoch_utc": "TO_FILL_AT_SUBMISSION",
    "register": "TO_FILL_AT_SUBMISSION",
    "window_id": "TO_FILL_AT_SUBMISSION"
   },
   "template_joint_isa_2q": "FILL_AT_TRANSPILE_FREEZE — MEASURED per rung on the real coupling map with the line layout actually supplied (epoch-independent geometry). No closed form exists for a buildable layout; deriving it was the error (#6541).",
   "u_est": "TO_FILL_AT_SUBMISSION (= exp(-lambda * template_joint_isa_2q); grader recomputes and refuses on mismatch)",
   "layout_spec": "SUPERSEDED->LINE (Whisper #6541: the two-rail ladder is UNCONSTRUCTIBLE on heavy-hex — ZERO 4-cycles in the coupling graph, by design; ladder_layout kept in-kit as a documented impossibility). Q arm flies the LINE layout supplied explicitly via line_layout(cm, 2n) (backtracking, fails loudly). Bell pairs therefore route; the cost is NOT closed-form and MUST BE MEASURED.",
   "template_joint_isa_2q_expected_note": "ONE-SIDED sanity check only: the measured count must be >= 276 (the unconstructible-ladder bound = swap networks + n Bell edges with zero routing — any measured count BELOW it is impossible and REFUSES). The former exact-equality requirement (103404e) pinned an impossible layout and is SUPERSEDED — it was fail-safe (refused everything) but unsatisfiable. Direction of the derivation error was FLATTERING (understated cost -> over-predicted u -> optimistic tau_Q); it never froze because the slot was empty — fill discipline 3-for-3.",
   "template_joint_isa_2q_lower_bound": 276
  },
  {
   "n": 16,
   "tau_C1": 0.71,
   "tau_C1_provenance": {
    "p_ALT": 0.42,
    "se_ALT": 0.0156,
    "p_NULL": 1.0,
    "se_NULL_upper": 0.001,
    "R": 1000,
    "instrument": "stabilizer-sampling (#6472 option d)",
    "artifact": "results/doora_tau_c1_freeze_whisper_c5027.json"
   },
   "copies_per_round": 64,
   "C1_round_grid": [
    1,
    2,
    3,
    4
   ],
   "tau_Q_rule": "midpoint of (p0 = 1/2 + 2^-17, p1 = (1 + u_est)/2), u_est from template ISA count x epoch lambda",
   "tau_Q": "TO_FILL_AT_SUBMISSION",
   "Q_pair_grid": "TO_FILL_AT_SUBMISSION",
   "Q_noise_pred": "TO_FILL_AT_SUBMISSION",
   "C1_noise_pred": "TO_FILL_AT_SUBMISSION",
   "lambda_provenance": {
    "lambda": "TO_FILL_AT_SUBMISSION",
    "epoch_utc": "TO_FILL_AT_SUBMISSION",
    "register": "TO_FILL_AT_SUBMISSION",
    "window_id": "TO_FILL_AT_SUBMISSION"
   },
   "template_joint_isa_2q": "FILL_AT_TRANSPILE_FREEZE — MEASURED per rung on the real coupling map with the line layout actually supplied (epoch-independent geometry). No closed form exists for a buildable layout; deriving it was the error (#6541).",
   "u_est": "TO_FILL_AT_SUBMISSION (= exp(-lambda * template_joint_isa_2q); grader recomputes and refuses on mismatch)",
   "layout_spec": "SUPERSEDED->LINE (Whisper #6541: the two-rail ladder is UNCONSTRUCTIBLE on heavy-hex — ZERO 4-cycles in the coupling graph, by design; ladder_layout kept in-kit as a documented impossibility). Q arm flies the LINE layout supplied explicitly via line_layout(cm, 2n) (backtracking, fails loudly). Bell pairs therefore route; the cost is NOT closed-form and MUST BE MEASURED.",
   "template_joint_isa_2q_expected_note": "ONE-SIDED sanity check only: the measured count must be >= 496 (the unconstructible-ladder bound = swap networks + n Bell edges with zero routing — any measured count BELOW it is impossible and REFUSES). The former exact-equality requirement (103404e) pinned an impossible layout and is SUPERSEDED — it was fail-safe (refused everything) but unsatisfiable. Direction of the derivation error was FLATTERING (understated cost -> over-predicted u -> optimistic tau_Q); it never froze because the slot was empty — fill discipline 3-for-3.",
   "template_joint_isa_2q_lower_bound": 496
  }
 ],
 "interpretation_bindings": [
  "sim gap is a CEILING on the flown gap (hardware noise raises p_ALT toward p_NULL) — the headline may not quote sim gaps as flown properties",
  "WIN per the redrafted §4: Q excess consistent with 0 AND C1 excess consistent with 1 excluding 0, vs per-rung noise-only predictions at each rung's own epoch lambda",
  "raw slopes inadmissible (Q noise-only exponent exceeds C1's — raw reading inverts the separation)",
  "kill criteria 1-5 per card; a rung dropped on shot budget is dropped and reported"
 ]
}