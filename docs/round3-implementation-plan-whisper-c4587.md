# Round-3 Implementation Plan — v2 (second-pass revised)

**Author**: Whisper (DC15W), C4587 (2026-07-12). Creator-directed: plan everything in
`gaps-and-connections-round3-whisper-c4586.md` plus queue item 0, second-pass the plan, then
implement as much as possible while Exp108b (`d998ch0tcv6s73dmvqr0`) queues.

**Recon findings this cycle (inputs to the plan)**:
- `causal-learn` 0.1.4.8 installed to the user site (same location as the qiskit stack).
- Paper draft = 316 lines; blocker sites located (0.9098 at lines 18/314 context, σ at 134–140).
- Raw per-shot counts are NOT retained for exp105–108; measured JOINT aggregates are
  (`exp106_hw_results.json` p_plus per branch/input, `exp108_grade.json` p1|±, P±) — the stress
  dataset must be a **declared parametric bootstrap** from measured joints, not raw shots.
- fig15 is generated by `scripts/generate_figures.py:486` — the relabel is a code edit + rerun.

**Scope boundary**: this plan covers the round-3 doc's items plus queue item 0 (paper
blockers). Prior-round backlog (switch-bench, weather page, memory sentinel, unified ledger,
Φ×ICO, deep-canary) is EXCLUDED except where subsumed (P4 consumes the atlas; the weather page
becomes cheaper after P4 formats the atlas for humans). Exp108b grading preempts everything on
arrival (frozen rule).

## Cross-cutting rules (pre-dev)

1. **Naming**: describe what a thing IS; no "honest-X" names (Creator C4579).
2. **Commits**: one commit per work item, quantum repo, message states what changed + evidence.
3. **Verification**: every script runs from committed inputs (no numbers typed from memory into
   docs — docs quote script output); P6-class pages get render-verified before commit.
4. **Confirmation-symmetry (C4483)**: P1's audit classifies MY findings by the same rules as
   siblings'; evidence pointers mandatory.
5. **Pre-registration inward**: where an item tests a hypothesis about ourselves (P1), the
   hypothesis is frozen in the report header BEFORE classification begins.
6. **Outward actions**: nothing leaves the repo (issue filing, emails) without Creator approval;
   drafts only.
7. **Rollback**: per-item commits make `git revert` the rollback path; no cross-item file
   coupling.

---

## Work items

### P0 — Pearl-bridge blocker pass (Elder C6443 review) [top priority, pre-existing queue]

**Blocker 1 (two ceilings)**: make pre-registered 0.8695 the ONE operative ceiling everywhere
(abstract, §3, §5 table); present class-balanced 0.9098 in text as the alternate measure with σ
against BOTH; regenerate fig15 with the 0.8695 line as "THE CEILING" (0.9098 as secondary
dashed line, labeled class-balanced).
**Blocker 2 (σ provenance)**: disclose the SE formula; report stat-only AND stat+syst σ;
degrade headline claims to ">100σ under any error model". Reconcile 216.8 vs the 214.9
recomputed from stated numbers (disclose SE≈0.000495 or restate).
**Pass-level items**: §7 one sentence (N=3 shows "above zero", not positive scaling); trim F80
"retracted by its own author before running" phrasing; state the 0.3pp subtraction
(|0.9769−0.9738|); move the what-is-new sentence adjacent to the photonic-DI concession.
**Acceptance**: no undifferentiated 0.9098; ALL σ values recomputed programmatically from the
disclosed SE formula (inline check, not by hand) and consistent with the text; fig15
regenerated and committed; version bumped to v0.4 with a changelog block; Discord re-review
request to Elder.
**Risk**: fig regeneration breaks other figures — read fig15's current code first, regenerate
fig15 only.

### P1 — Findings status-ledger + self-replication audit v1

**Deliverables**: `findings/status-ledger.json`; `tools/replication_audit.py`;
`docs/self-replication-audit-v1.md`.
**Schema** (one row per finding): `id`, `file`, `arc`, `claim_type`
(direction | magnitude | law | rate | existence), `status` (CONFIRMED_ON_RETEST | UNTESTED |
SOFTENED | REFUTED | REGIME_CONTINGENT | RETRACTED_PRE_RUN), `retested` (bool), `retest_ref`,
`sim_only` (bool), `single_run` (bool), `evidence` (file/line pointer justifying status).
**Scope v1**: NUMBERED findings only — catalog Findings 1–27 (statuses derivable from catalog
text), numbered 28–44, F-series F75–F86 — plus the 7 documented reversals. Preregs, results
JSONs, and unnumbered notes in `findings/` are NOT findings and stay out of the ledger.
Everything in-scope but unclassifiable enters as UNTESTED.
**Hypothesis freeze (rule 5)**: BEFORE classification, the report header states: "H1:
magnitude/rate/law claims are refuted or softened at a higher rate than direction/existence
claims when retested." Grading criteria fixed with it.
**Audit**: survival rates by claim_type among RETESTED findings only (untested findings can't
inform survival); Fisher exact on claim_type × (refuted|softened) where n permits; report n per
cell, no rate quoted without its n attached.
**Acceptance**: every non-UNTESTED status carries an evidence pointer; script runs from ledger
alone; hypothesis frozen before classification; sibling spot-check of 10 random rows requested
via Discord.
**Risk**: self-classification bias (I authored many findings). Mitigation: evidence pointers
mandatory + invite sibling spot-check of 10 random rows via Discord.

### P2 — Maxwell-demon ledger (F86 bookkeeping)

**Deliverables**: `tools/demon_ledger.py`; `docs/maxwell-demon-ledger-whisper-c4587.md`.
**Method**: from `exp108_grade.json` measured values — reservoir τ=diag(0.75,0.25) →
ΔE/kT = ln3 (Boltzmann); baseline p_th = the MEASURED thermalization nulls (0.2496/0.2493),
not the nominal 0.25 (measured everywhere possible); heat extracted on + outcome
ΔQ₊ = (p_th − p1|+)·ΔE; heat dumped on −;
control measurement information = H(P₊) bits; Landauer floor for erasure = kT·ln2·H(P₊);
efficiency = heat moved / Landauer cost; second-law balance check (net entropy ≥ 0).
**Verification**: cross-check accounting definitions against Felce–Vedral (PRL 125) if the
paper is in `sources/`; otherwise label the accounting as Landauer-standard and flag F-V
definition check as pending.
**Acceptance**: script reproduces all inputs from the grade JSON; every assumption labeled;
second-law check passes; same script re-runs on Exp108b grade when it lands (native-fluid row).
**Risk**: wrong accounting convention → mitigation: state convention explicitly, present ratios
not absolute joules, flag as v1 pending F-V cross-check.

### P3 — Causal-discovery stress-test dataset

**Deliverables**: `tools/causal_stress_dataset.py` (seeded parametric bootstrap from measured
joints); dataset CSV in `results/`; `tools/run_causal_discovery.py` (PC + GES via causal-learn);
`docs/causal-discovery-stress-test-whisper-c4587.md`.
**Design v1 (Exp108 only)**: variables S (setting: switch / null_fwd / null_rev), C (control
±), T (target 0/1); joint per setting reconstructed as P(C)·P(T|C) from the grade JSON (switch:
P±, p1|±; nulls: control is deterministic-≈1 in +, so C carries no information there — state
this). Sample N=50k shots per setting, fixed seed. Run PC (chi-square CI test) and GES
(discrete BIC). **Extension noted, not v1**: add Exp106 (input bit B as a 4th variable) once
the v1 blindness result is written.
**The honest claim** (calibrated in the round-3 doc, restated here): NOT "algorithms fail" —
they will return an ordinary DAG. The demonstration is **blindness**: the same data that
certifies no-definite-order at 216.8σ (game) / 21.1σ (thermal) looks entirely normal to
observational causal discovery, which silently assumes a definite order exists. Two analyses,
one dataset, opposite verdicts about the premise.
**Acceptance**: seeded + reproducible; PC and GES outputs recorded verbatim; writeup states the
blindness framing with no overclaim; dataset published as CSV with a README block.
**Risk**: causal-learn API instability → pin version in the doc (0.1.4.8).

### P4 — Upstream FakeMarrakesh depth-optimism issue (DRAFT ONLY)

**Deliverable**: `docs/upstream/fakemarrakesh-depth-optimism-issue-draft.md` — issue body built
from the C4564 atlas: ln-optimism +0.04 shallow → +0.21–0.31 deep, observable-family-dependent,
3 independently flagged incidents, repro pointers (job IDs, prereg commits), environment
(qiskit-ibm-runtime 0.47.0).
**NOT filed** — filing is outward-facing; Creator approves first.
**Known-rebuttals section required** (pre-empt the maintainer's easy outs): "snapshot models
drift from live hardware — expected" → our claim is the DEPTH-SCALING of the optimism, not its
existence, and atlas rows carry same-window sentinel evidence separating window quality from
model error.
**Acceptance**: body complete enough to paste; includes what-we-ask (depth-dependent error
characterization in fake-provider docs or model correction), environment versions, repro
pointers, and the rebuttals section.

### P5 — Vacuous-gate linter extraction

**Deliverable**: `tools/gate_feasibility_lint.py` — standalone.
**Input API** (JSON): `{"shots": int, "gates": [{"name", "null_value" (value of the gate
statistic under pure noise), "threshold", "direction" ("above"|"below"), "se_at_budget"}]}` →
per-gate verdict: CAN-PASS / CAN-FAIL / VACUOUS-PASS (cannot fail at 5SE) / VACUOUS-FAIL
(cannot pass at 5SE), with margins.
**Test case**: the Exp108b pre-freeze defect (therm-gate 5SE=0.069 > 0.05 band → vacuously
impossible) must be caught — numbers read from `results/exp108b_feasibility.json`, not retyped.
**Acceptance**: reproduces the Exp108b catch from the recorded file.

### P6 — Pearl Ladder tour page

**Deliverable**: `demo/ladder/index.html` — static, self-contained, existing demo styling.
**Content outline**: Stop 1 (Rung 1, association): characterization corpus, noise maps.
Stop 2 (Rung 2, intervention): Finding 11 dose-response do() + Finding 18 H-removal do(), real
numbers. Stop 3 (Rung 3, counterfactuals): prereg discipline as counterfactual commitment
(frozen predictions vs outcomes). Stop 4 (off the ladder): the switch, 216.8σ, ladder premise
fails. **Largest build — implement last; defer to next cycle if budget runs out.**

## Execution order and gating

| Order | Item | Depends on | Est. effort |
|---|---|---|---|
| 1 | P0 paper blockers | — | medium |
| 2 | P5 linter | — | small |
| 3 | P2 demon ledger | exp108_grade.json (have) | medium |
| 4 | P1 status-ledger + audit | reading findings | large |
| 5 | P4 upstream issue draft | atlas (have) | small |
| 6 | P3 stress dataset | causal-learn (installed) | medium |
| 7 | P6 ladder page | — | large |

**Exp108b polling**: check job status after each item's commit; if it lands, its grade preempts
everything (frozen rule), then P2's script gains its second row.

---

## Second-pass changelog (v1 → v2)

Gap fixes: scope boundary vs prior-round backlog stated; cross-cutting pre-dev rules added
(naming, per-item commits, no-retyped-numbers verification, confirmation-symmetry, outward-
action gating, rollback); P0 gains programmatic σ-consistency check + v0.4 changelog
requirement; P1 gains hypothesis-freeze-before-classification (our prereg discipline pointed
inward), a findings-vs-non-findings scope rule, and retested-only survival denominators; P2
baseline switched from nominal 0.25 to the MEASURED thermalization nulls; P3 variable design
specified concretely (Exp108-only v1, deterministic-control caveat for null arms, Exp106
extension deferred); P4 gains a known-rebuttals section requirement; P5 gains an input API and
a no-retyping rule (reads the recorded feasibility JSON).
