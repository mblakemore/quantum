# H13 Cell 8 — THE SWITCH UNDER OATH (revised spec) — what is banked, what is walled, what is genuinely open

**Author**: Whisper (DC15W), C5053 (2026-08-10) · **Substrate**: claude-fable-5
**Creator GO**: Side-B menu item (c) (board #55, "c go!"). $0 deliverable — spec only; any flight is separately gated.
**Status of the idea, corrected twice by the F-arc tool**: my C5049 menu line ("switch-as-instruction: one-query task vs enumerated two-query floor — open") was **stale on both halves**, and this spec's first job is to say so precisely.

## 0. The correction that reshapes the cell (rediscovery record, on the page per house rules)

Two F-arc passes with *task-shaped* (not topic-shaped) query terms surfaced what the C5049 topic-framing missed:

- **The task column is BANKED.** `switch-as-computer-scout-whisper-c4999` (my own $0 scout, 16 days old): **F82** already plays the discrimination game on hardware — **0.9769 (marrakesh) / 0.9738 (fez) against the in-house SDP causally-separable ceiling 0.8690** (fixed + mixed + *dynamical* definite order, **computed not cited**), 216.8σ. The "switch as an instruction graded against an enumerated definite-order floor" exists at the process-abstraction level, on two chips.
- **The strong access model is WALLED.** Scout Finding 2: a fixed gate-model circuit cannot enforce *single physical firing* of each black box in both orders — gate-model switches compile controlled-U (two controlled applications); "one use each" is the time-delocalized-systems *interpretation* (Oreshkov), not an enforced restriction. With c-U access granted, definite-order circuits can reproduce the statistics, so the **enforced-access Chiribella separation cannot exist on this hardware class** (photonic path-superposition is the platform that enforces it). The gate-teleportation escape was probed and **fails for blind decoding** (Finding 3, sign scrambled by correction phases — circular).
- **F75** (Elder, hardware) holds the order-coherence witness (W = +1.781, 3/3), with its own caveat №3 naming a real open hardware item: **the classical 50/50 mixture-of-definite-orders adversary was beaten only in SIM (F73, W₂ = +1.93 FakeMarrakesh) — never on silicon.**

**Method lesson (banked as a pattern)**: F-arc recall is framing-sensitive — C5049's topic query ("quantum switch indefinite causal order hardware") found F73/F75 but not the scout; today's task query ("channel discrimination commute anticommute one query") found it at score 10.7. Run the *task-shaped* framing at proposal time, not only at delivery time.

## 1. What Cell 8 therefore IS: three rungs of honest remainder

### Rung 1 — THE MIXTURE ARM ON SILICON (closes F75 caveat №3) — the only new physics flight here

**What**: fly the sharper causally-separable adversary on hardware — the classical 50/50 mixture of the two definite orders — alongside (or against banked) F75's switch arm, in one window.
**Why it matters**: F75's hardware comparison was switch-vs-*pure*-definite; the mixture is the adversary the causal-nonseparability claim actually needs beaten on silicon. F73 predicts W₂ ≈ +1.93 under the noise model; hardware precedent (F75) took an ~8% haircut from its noise-model prediction.
**Apparatus**: Exp91's flown circuits + F73's mixture recipe (job-level classical mixing of the two definite-order PUBs — no new circuit class); quiet-qubit picker fresh (never cached).
**Certifies as**: W₂(hardware) over the mixture ceiling at ≥5σ, bands frozen from F73's sim ± the F75-measured haircut envelope; run-to-run drift bar ±0.07 inherited.
**Price**: cheap — ~6–8 PUBs × 6000 shots, single window. **This is Elder's lineage (Exp91/F73/F75) — his seat leads; mine assists.** Flag: competes for any small tank with Cell 6+6b.

### Rung 2 — THE SEALED-COURT FORM OF THE BANKED GAME (rigor upgrade, no new physics claim)

**What**: the two cheap upgrades the C4999 scout named and nobody executed: (a) **blind sealed-court form** of the F82 game — Ember-sealed instance sequence, frozen public grader, blind decode, reveal against commitment (the race-court rigor F122 established as house standard); (b) **explicit computational-task scoreboard framing**: "COMMUTE(A,B) decided in one *controlled*-query of each, at the process-abstraction level, device-characterized scope."
**Advantage apparatus, in full** (this is the arc's second advantage-class object after Cell 2): claim card with floor fields; the **SDP ceiling 0.8690 re-derived in-code at freeze** (never cited from F82 — F87 discipline); `attack_preflight.py` all classes **plus the pending billing-currency class** (both arms in the same query currency: controlled-calls, identically counted); 3-of-3 court.
**Certifies as**: blind hardware success over the re-derived ceiling at ≥5σ under seal, with the scoreboard row carrying the scope label verbatim.
**Price**: cheap-mid (mostly court labor, small QPU re-fly for the sealed instances).

### Rung 3 (optional instrument) — CONSTANT OR WEATHER?

Re-measure the F82 game margin and/or W across ≥2 calibration epochs: is the switch's operational signature a **constant** of the device class or **weather**? First purpose-built causal-structure datapoint for the H-program's constants-vs-weather taxonomy (shares machinery with Cell 7's second window). Price: piggyback-cheap.

## 2. Access-model honesty (the wall, stated where the headline will live)

Every Cell 8 number carries this scope, in the same breath: *the chip is a fixed-causal-order processor; the switch is realized by controlled routing; causal nonseparability is a property of the effective process (standard for all photonic/superconducting demos); the query currency is controlled-calls under a device-characterized access model. The enforced single-firing access model of the Chiribella theorem is physically unavailable on this hardware class (C4999 scout, Finding 2) and is never claimed. DI certification of the switch is provably impossible (Bavaresco 2019; C4590), SDI is photonic-only to date.*

## 3. What Cell 8 is NOT

- Not a new column: the discrimination-game result is F82's; Rung 2 upgrades its **rigor and framing**, and says so on the scoreboard.
- Not an enforced-one-query separation (walled), not physical indefinite order in the silicon, not DI/SDI.
- Rung 1 is a **witness-tightening** (foundations genre, no claim card); only Rung 2 is advantage-class and carries the full apparatus.

## 4. Gates before any flight

G0 (freeze): SDP ceiling re-derivation in-code matches 0.8690 to tolerance; mixture-arm bands frozen from F73 + haircut envelope; billing-currency preflight class adopted by the court (or Rung 2 waits). G1: `preflight_account_check.py` on every submission script. G2: fit-gate at submit vs whatever tank is live — never asserted from a balance. Seats: Elder leads Rung 1 (his lineage) and the grader seat; Ember seals Rung 2; Whisper drives the spec, the in-code ceiling re-derivation, and the scoreboard framing.

---

*The switch already testified (F75) and already won its game (F82). Cell 8 puts it under oath: the sharper adversary on real silicon, the win re-earned blind, and the scope printed where the applause would go.*
