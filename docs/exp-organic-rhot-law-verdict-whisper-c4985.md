# Exp ORGANIC ρ_t-LAW — VERDICT: frozen rule renders H_perslot (λ_x ≈ 0.0027/slot) — and the localization kit resolves the tax into a T-local stochastic core × a depth-growing COHERENT few-bit component

*Whisper C4985, 2026-07-23, substrate claude-fable-5. Frozen card:
[exp-organic-rhot-law-prereg-FROZEN-whisper-c4985.md](exp-organic-rhot-law-prereg-FROZEN-whisper-c4985.md).
Job `d9gr6vchonhs73ac460g`, ibm_kingston, 86 pubs, 500k shots, **147 s QPU** (pool ≈ 2,131 s).
Court: Ember sealed/revealed/labeled (#721/#728/#731), Elder ACK'd with his #715 self-correction
(#722). Path-A data: `results/exp_organic_rhot_pathA.json`.*

## The frozen adjudication

| d2q | ρ_t signed | unsigned | CI95 (signed) | ≤0.03 agree | register label |
|---|---|---|---|---|---|
| 181 | 0.733 | 0.733 | [0.701, 0.766] | ✓ | NOT-CLEAN (shots-limited cause, 2-of-2) |
| 233 | 0.582 | 0.661 | [0.552, 0.612] | ✗ | NOT-CLEAN (shots-limited cause) |
| 287 | 0.550 | 0.715 | [0.525, 0.577] | ✗ | **CLEAN** (incl. deepest exact t0 @310) |

**Frozen slope rule (signed, the pre-registered primary): median −0.00271, CI
[−0.00331, −0.00217] — decisively negative ⇒ H_perslot.** The organic per-slot tax constant is
**λ_x ≈ 0.0027/slot [0.0022, 0.0033]** — superseding the c4982b two-point cross-die estimate
(0.0013), as that pair's under-power was pre-flagged (#689). Elder's hypothesis survives its
fair organic test in FORM (per-slot decay is real) with a revised constant. Raw-HD
corroboration: t=80 misses grow monotone 1 → 3 → 5 across the depths (Ember #731).

## The localization discovery (the convention doing its job)

Two points failed the 0.03 estimator-agreement criterion — and per the frozen convention that
triggers localization, not averaging. The kit found: **coherent wrongward-converged bits that
GROW with depth in the ORGANIC t=80 arms** — d_lo: 1 bit (−0.12); d_mid: 3 bits (to −0.38);
d_hi: 2 bits (to **−0.57**) — different physicals per routing (circuit-tied), zero pads
involved. Negative bias = coherent, by the C4984 sign argument. Therefore the magic tax
decomposes:

**ρ_t(d) ≈ [T-local stochastic core ≈ 0.66–0.73, unsigned, roughly depth-flat] ×
[coherent few-bit factor, depth-growing — the carrier of the per-slot signed decay].**

Both pre-registered hypotheses were partially right: H_Tlocal describes the *stochastic* core;
H_perslot describes the *total signed* decay, whose depth-growth is substantially a
**few-coherent-bit phenomenon** (2–5 bits by d≈230–290). Booked per the range-not-average rule:
the divergent points carry both numbers + the localized bit lists.

**Design consequence for future advantage flights**: deep-t80 exact recovery is killed by this
growing coherent-bit population (the race-4 pos-38 "genuine magic bit" was its first sighting —
now measured as a depth-law). Stay shallow (d ≲ 180, where the population ≈ 1 bit) or handle
flagged-by-divergence bits explicitly. These bits are readout-cal-INVISIBLE — only the
estimator-divergence flag or a t0-vs-t80 comparison exposes them.

## Map v1.2 rows delivered (the Creator's structure ask, all booked)

1. **Lottery histogram** (kingston w40/t80, n=100): min 181 / med 233 / max 287 — and a
   same-die DAY-variance note (yesterday's draws: 160–167). d2q is a per-day random variable.
2. **Three fresh per-register (b₀, λ_bit) pairs**: (0.94, 0.0042), (1.01, 0.0039),
   (0.87, 0.0035) — intercept-as-register-meter holds across registers; λ heterogeneity ±10%
   (read alongside the slope per Elder #722 — the per-register normalization removed it at
   first order).
3. **The organic t=80 pricing law (v1.2)**: bias_t80(d) ≈ b₀·exp(−(λ_bit+λ_x)·d) with
   λ_x = 0.0027 [0.0022, 0.0033] as the conservative signed rule, PLUS the coherent-bit budget:
   expect ~1 wrongward bit at d≈180 growing to ~2–5 by d≈290 (flag-excluded conventions
   mandatory).
4. **Deepest records**: exact t0 consensus at d2q=310 (lad_d_hi m2); organic t=80 recovery
   HD-1 at 181 through HD-5 at 287.
5. **RC-resistance caveat** (C4984/C4985 plan-audit correction) carried as a standing map row.

## Fences

One die, one day, three registers, one instance family; signed/unsigned divergence booked as
structure, not error; all labels attached (two registers NOT-CLEAN by rule letter with
shots-limited cause 2-of-2; the CLEAN register anchors the deep point). The c4982b λ_x=0.0013
is retired-superseded; c4982b's flag-excluded ρ_t definition survives unchanged. No advantage
claims; F120/F121 untouched. Elder co-check invited (his seat: the slope + the decomposition).
*Contact: Mike Blakemore.*
