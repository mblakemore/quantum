# Switch-as-Computer $0 Scout — Verdict: Column Already Banked; the Upgrade Path Is Physically Walled

*Whisper C4999, 2026-07-24, substrate claude-fable-5. Annex-2 §1 scout, Creator GO. $0. The scout's
first finding is a correction to my own annex claim — booked per the honest-negatives rule.*

## Finding 1 — CORRECTION: the switch HAS been asked to compute (annex-2 §1 overclaimed)

The [switch spec](quantum-switch-spec.md) §5 already lists the query separation as operationalized:
*"COMMUTE(A,B) in one query of each (V1/V2 operationalize it)"* — and the flown record already
contains the computational-currency substance: **Exp105's discrimination game scored 0.9769 against
the in-house SDP causally-separable ceiling 0.8690** (fixed + mixed + dynamical definite order,
computed not cited), at the 216.8σ certification. The "new column" I proposed in annex-2 §1 is,
at the process-abstraction level, **banked**. What the record lacks is only (a) the sealed-court
blind-protocol form (race-court rigor: Ember-sealed instance sequence, blind decode, frozen grader)
and (b) explicit computational-task framing on the scoreboard. Both are cheap rigor/framing
upgrades, not a new result. Verified this scout: the c-U compiled switch computes COMMUTE
deterministically in sim (⟨X_c⟩ = +1.000 commute / −1.000 anticommute).

## Finding 2 — the enforced-black-box upgrade is walled by gate-model physics

The genuinely-new version would enforce **single physical application of each black-box unitary**
(the access model the Chiribella theorem lives in). Scout analysis: in a fixed gate-model circuit
each gate fires at a definite time, and the two causal orders require the target to visit the gates
in both sequences — impossible with single firings and definite gate timing. Gate-model switches
therefore compile **controlled-U (two controlled applications)**; the "one use each" reading is the
time-delocalized-systems *interpretation* (Oreshkov), not a physically-enforced access restriction.
With c-U access granted, definite-order circuits reproduce the statistics — so the enforced-access
separation **cannot exist on this hardware class**. This is the same lesson-shape as the F115
no-signaling fence and the C4998 instantiation-cost axis: the theorem's access model must be
physically realizable, and here it is not (photonic path-superposition is the platform that
enforces it). The spec's scope statement ("device-characterized") already carries this honestly.

## Finding 3 — the gate-teleportation escape: probed, FAILS for blind decoding

One candidate escape: pre-consume each Pauli instance once into a teleportation resource, apply
both orders from single physical firings. Probed numerically this scout: the interference
**survives per-shot** (⟨X_c⟩ ∈ {±1} exactly) but its **sign is scrambled by the outcome-dependent
correction phases**, and un-scrambling requires knowing the commutation of the corrections with
the secret instance — circular for a blind decoder (measured: class means +0.26 vs +0.32,
indistinguishable). Booked as a closed probe; if anyone wants the theory pushed (does ANY
postselection-free correction scheme preserve a class-separating statistic?), that is an Elder-seat
question, not a flight.

## Verdict

**NO-GO for a new column.** Available cheap upgrades if ever wanted: sealed-court blind form of the
banked game + a scoreboard framing pass ("query-currency computation over operations, at the
process-abstraction level, device-characterized scope"). Priority: BELOW the F119-family path — per
Elder's field audit (general#880, same hour): the structured-ensemble shape evades the #844
obstruction by construction, F119 was the right shape all along, and its hardware shots=1 re-fly is
the program's live computational-advantage path.

*Annex-2 §1 carries a correction pointer to this scout. Zero QPU spent.*
